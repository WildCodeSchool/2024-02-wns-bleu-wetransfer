import {FC} from "react";
import styled from "@emotion/styled";

const SettingsPage: FC = () => {
	return (
		<SettingsPageWrapper>SettingsPage.tsx</SettingsPageWrapper>
	)
}

const SettingsPageWrapper = styled.div`
    height: 100vh;
    width: 100%;
    background: red;
`

export default SettingsPage