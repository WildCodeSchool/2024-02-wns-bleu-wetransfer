import styled from "@emotion/styled";

const PricingPage = () => {
	return (
		<PricingPageWrapper>Pricing page</PricingPageWrapper>
	)
}

const PricingPageWrapper = styled.div`
    height: 100vh;
    width: 100%;
    background: #2a81c0;
`

export default PricingPage